import {ArrowUpRight,Star} from 'lucide-react'
import {Link} from 'react-router-dom'
import type {Product} from '../data/products'
import {useStore} from '../store/Store'
export function ProductCard({product}:{product:Product}){const{locale}=useStore();return <Link to={`/product/${product.id}`} className="group block"><div className="relative aspect-[4/4.5] overflow-hidden rounded-2xl" style={{background:product.color}}><img src="/collection.png" alt={locale==='en'?product.name:product.namePt} className="h-full w-full scale-[1.65] object-cover transition duration-500 group-hover:scale-[1.72]" style={{objectPosition:product.position}}/><span className="absolute right-3 top-3 grid h-9 w-9 place-items-center rounded-full bg-white/90 opacity-0 transition group-hover:opacity-100"><ArrowUpRight size={17}/></span></div><div className="mt-3 flex items-start"><div><h3 className="font-bold">{locale==='en'?product.name:product.namePt}</h3><p className="mt-1 text-sm text-slate-500">{locale==='en'?product.category:product.categoryPt}</p></div><div className="ml-auto text-right"><b>${product.price}</b><p className="mt-1 flex items-center justify-end gap-1 text-xs text-slate-500"><Star size={12} fill="currentColor"/>{product.rating}</p></div></div></Link>}
