import {ArrowRight,Search,SlidersHorizontal} from 'lucide-react'
import {useMemo,useState} from 'react'
import {ProductCard} from '../components/ProductCard'
import {categories,products} from '../data/products'
import {useStore} from '../store/Store'
export function HomePage(){const{locale}=useStore();const[q,setQ]=useState('');const[category,setCategory]=useState('All');const[sort,setSort]=useState('featured');const visible=useMemo(()=>products.filter(p=>(category==='All'||p.category===category)&&(p.name+p.namePt).toLowerCase().includes(q.toLowerCase())).sort((a,b)=>sort==='low'?a.price-b.price:sort==='high'?b.price-a.price:b.rating-a.rating),[q,category,sort]);return <><section className="mx-auto max-w-7xl px-4 pt-5 md:px-8 md:pt-8"><div className="hero"><img src="/collection.png" alt="Curated everyday products"/><div className="hero-copy"><p className="eyebrow">{locale==='en'?'New collection · 2026':'Nova coleção · 2026'}</p><h1>{locale==='en'?'Everyday, considered.':'O cotidiano, repensado.'}</h1><p>{locale==='en'?'Objects with purpose, selected for how you really live.':'Objetos com propósito, escolhidos para o seu jeito de viver.'}</p><a href="#shop" className="btn-dark">{locale==='en'?'Explore the edit':'Explorar coleção'} <ArrowRight size={17}/></a></div></div></section><section id="shop" className="mx-auto max-w-7xl px-4 py-16 md:px-8"><div className="mb-8 flex flex-wrap items-end justify-between gap-4"><div><p className="eyebrow">{locale==='en'?'The collection':'A coleção'}</p><h2 className="section-title">{locale==='en'?'Made for modern life':'Feito para a vida moderna'}</h2></div><p className="max-w-md text-sm leading-6 text-slate-500">{locale==='en'?'Fewer, better things. Useful essentials with quiet character.':'Menos coisas, melhores escolhas. Essenciais úteis e cheios de personalidade.'}</p></div><div className="mb-7 grid gap-3 lg:grid-cols-[1fr_auto_auto]"><label className="relative"><Search className="absolute left-4 top-3.5 text-slate-400" size={18}/><input className="field pl-11" value={q} onChange={e=>setQ(e.target.value)} placeholder={locale==='en'?'Search the collection':'Buscar na coleção'}/></label><div className="flex gap-2 overflow-x-auto">{categories.map(c=><button key={c} onClick={()=>setCategory(c)} className={`chip ${category===c?'active':''}`}>{c}</button>)}</div><label className="relative"><SlidersHorizontal className="absolute left-3 top-3.5" size={16}/><select className="field pl-9 pr-8" value={sort} onChange={e=>setSort(e.target.value)}><option value="featured">{locale==='en'?'Featured':'Destaques'}</option><option value="low">{locale==='en'?'Price: low':'Menor preço'}</option><option value="high">{locale==='en'?'Price: high':'Maior preço'}</option></select></label></div><div className="grid gap-x-5 gap-y-10 sm:grid-cols-2 lg:grid-cols-3">{visible.map(p=><ProductCard key={p.id} product={p}/>)}</div>{!visible.length&&<div className="py-20 text-center"><b>{locale==='en'?'No products found':'Nenhum produto encontrado'}</b></div>}</section></>}
